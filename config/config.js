export const GLOBAL = {
    frameRate: 60,
    lineSpacing: .07,
    lineLength: .01
};

export const LINESET = {
    thetaMin: 0,
    thetaMax: 0
};

export const SCREEN = {
    offsetX: window.innerWidth / 2,
    offsetY: window.innerHeight / 1.5,
    scaleX: window.innerHeight / 2,
    scaleY: window.innerHeight / 2,
    camY: 2,
    camZ: -2.5
}
